import React, { useEffect, useRef } from 'react';
import { StaticImage } from 'gatsby-plugin-image';
import styled from 'styled-components';
import { srConfig } from '@config';
import sr from '@utils/sr';
import { usePrefersReducedMotion } from '@hooks';

const StyledAboutSection = styled.section`
  max-width: 900px;

  .inner {
    display: grid;
    grid-template-columns: 3fr 2fr;
    grid-gap: 50px;

    @media (max-width: 768px) {
      display: block;
    }
  }
`;
const StyledText = styled.div`
  ul.skills-list {
    display: grid;
    grid-template-columns: repeat(3, minmax(140px, 400px));
    grid-gap: 0 60px;
    padding: 0;
    margin: 20px 0 0 0;
    overflow: hidden;
    list-style: none;

    li {
      position: relative;
      margin-bottom: 10px;
      padding-left: 20px;
      font-family: var(--font-mono);
      font-size: var(--fz-xs);

      &:before {
        content: '▹';
        position: absolute;
        left: 0;
        color: var(--green);
        font-size: var(--fz-sm);
        line-height: 12px;
      }
    }
  }
`;
const StyledPic = styled.div`
  position: relative;
  max-width: 350px;

  @media (max-width: 768px) {
    margin: 50px auto 0;
    width: 70%;
  }

  .wrapper {
    ${({ theme }) => theme.mixins.boxShadow};
    display: block;
    position: relative;
    width: 100%;
    border-radius: var(--border-radius);
    background-color: var(--green);

    &:hover,
    &:focus {
      outline: 0;
      transform: translate(-4px, -4px);

      &:after {
        transform: translate(8px, 8px);
      }

      .img {
        filter: none;
        mix-blend-mode: normal;
      }
    }

    .img {
      position: relative;
      border-radius: var(--border-radius);
      mix-blend-mode: multiply;
      filter: grayscale(100%) contrast(1);
      transition: var(--transition);
    }

    &:before,
    &:after {
      content: '';
      display: block;
      position: absolute;
      width: 100%;
      height: 100%;
      border-radius: var(--border-radius);
      transition: var(--transition);
    }

    &:before {
      top: 0;
      left: 0;
      background-color: var(--navy);
      mix-blend-mode: screen;
    }

    &:after {
      border: 2px solid var(--green);
      top: 14px;
      left: 14px;
      z-index: -1;
    }
  }
`;

const About = () => {
  const revealContainer = useRef(null);
  const prefersReducedMotion = usePrefersReducedMotion();

  useEffect(() => {
    if (prefersReducedMotion) {
      return;
    }

    sr.reveal(revealContainer.current, srConfig());
  }, []);

  const skills = [
    'JavaScript/TypeScript',
    'Python',
    'React',
    'Redux-Toolkit',
    'Next.js',
    'Vue.js',
    'Angular',
    'Nest',
    'Django',
  ];

  return (
    <StyledAboutSection id="about" ref={revealContainer}>
      <h2 className="">About Me</h2>

      <div className="inner">
        <StyledText>
          <div>
            <p>
             I am a Senior Full Stack Developer with over 8 years of experience in web and blockchain development. My expertise spans across a wide range of technologies, including HTML5, CSS, JavaScript, TypeScript, React.js, Angular, Vue.js, and Solidity, among others. I have worked on diverse projects ranging from high-performance web applications to blockchain-based solutions like NFT dApps and decentralized finance (DeFi) platforms.
            </p>

            <p>
              II am passionate about building scalable, user-focused applications and have a strong foundation in both front-end and back-end development. I excel at gathering user requirements, producing clean and efficient code, creating intuitive designs, and managing the full development lifecycle. My ability to adapt to various technical environments has allowed me to deliver high-quality products that drive business success and provide valuable user experiences.
            </p>

            <p>
            In addition to my technical skills, I thrive in collaborative environments and am experienced with Agile methodologies, project management tools, and cloud platforms such as AWS and Azure. I am always eager to learn and stay up-to-date with the latest industry trends and technologies to continue delivering cutting-edge solutions.
            </p>

            <p>Here are my AREAS OF EXPERTISE :</p>
          </div>

          <ul className="skills-list">
            {skills && skills.map((skill, i) => <li key={i}>{skill}</li>)}
          </ul>
        </StyledText>

        <StyledPic>
          <div className="wrapper">
            <StaticImage
              className="img"
              src="../../images/me1.jpg"
              width={500}
              quality={95}
              formats={['AUTO', 'WEBP', 'AVIF']}
              alt="Headshot"
            />
          </div>
        </StyledPic>
      </div>
    </StyledAboutSection>
  );
};

export default About;
